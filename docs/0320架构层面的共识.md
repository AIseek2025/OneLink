**架构层面的共识**

"一度空间"项目，本质是：

人类关系图谱 + 长期上下文系统

\*\*OneLink 不是"AI 聊天 + 社交推荐"，而是"长期理解人的系统"。\*\*

最后一句话（核心本质）

你说得已经非常接近真相了，我帮你再压缩成一句：

OneLink 的核心不是"连接人"，

**而是"建模人"**。

而：

**Context / Memory OS = 人类建模引擎**

**必须优先解决的能力：**

长期记忆（核心）

用户画像

匹配算法

MiniMax → 主聊天引擎（长上下文）。MiniMax = OS（系统层）。

DeepSeek → 推理引擎（画像/匹配/判断）。DeepSeek = CPU（推理层）。

分工非常清晰：

**🟢 MiniMax 负责：**

用户聊天、长期记忆、上下文理解、问卷处理

因为：上下文超强、成本可控、工程稳定、好用

**DeepSeek 负责：**

用户画像推理、匹配推荐算法、风险识别、矛盾检测、RL优化

因为：推理能力更强、可训练性更好

**最后一句真话（不客气版）**

如果你只选一个模型：你这个项目大概率做不起来

因为：单模型做不好"聊天 + 推理 + 推荐"三件事

总体架构是不是应该是这样的：

User

↓

Chat Gateway

↓

Context Manager（上下文管理，最关键模块；职责：存用户长期记忆、提取
relevant context、控制 token；数据来源：聊天历史、问卷数据、用户画像）

↓

Task Router（任务路由）

↓

\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\--

\| 对话主模型 \|

\| 推理模型 \|

\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\--

↓

Result Aggregator（结果融合）

↓

Response

你不是在做：AI聊天产品

你在做：人类数字人格操作系统

而 Context Manager 是："**人格核心**"

**最后一句（很关键）**

如果你这个产品未来能赢：不是因为模型更强，

而是因为：你比别人更"懂用户是谁"

你的这个判断是对的，而且我会更直接一点说：

如果你把 Context Manager 当成一个"模块"，这个产品做不大；

如果你把它当成"核心基础设施（独立服务）"，才有机会走向 70 亿人。

下面我给你从架构层 / 数据层 / 性能层 / 演进路径完整拆清楚。

一、结论先说清楚（避免走弯路）

必须独立成微服务，而且要比其他服务更"重"

不是普通微服务，而是：

Memory OS（记忆操作系统级服务）

二、为什么 Context Manager 必须独立？

它是你真正的"产品核心"

你这个产品的本质不是：聊天、推荐、社交

而是：对"人"的长期理解能力

这个能力 100% 依赖：Context Manager

它的复杂度远超普通模块。

普通模块：CRUD、API调用。

Context Manager 要做：

长短期记忆管理

上下文压缩

向量检索

用户画像更新

冲突检测

权限隔离

多模型适配

这是一个系统，不是模块，它必须跨服务复用

以下模块都依赖它：

聊天系统

推荐系统

风控系统

用户画像系统

所以必须独立

三、正确架构（建议你用这个）

Context Manager 微服务架构

┌──────────────┐

│ Chat Service │

└──────┬───────┘

│

┌──────▼───────┐

│ Context API │ ←（统一入口）

└──────┬───────┘

┌─────────────────┼─────────────────┐

│ │ │

┌────▼────┐ ┌──────▼─────┐ ┌────▼────┐

│ Memory │ │ Vector DB │ │ Profile │

│ Store │ │ (Embedding)│ │ Engine │

└─────────┘ └────────────┘ └─────────┘

四、Context Manager 内部必须拆的 4 个子系统

Memory Store（记忆存储）

存：

原始聊天

问卷数据

行为记录

技术：

MongoDB / DynamoDB

Vector Memory（语义记忆）

存：

embedding

可检索上下文

技术：

Milvus / Pinecone / Weaviate

Memory Processor（最关键）

负责：

把"聊天"变成"知识"

包括：

摘要

结构化

去噪

分类

Profile Engine（画像引擎）

负责：

更新用户画像

冲突检测

信任评分

五、必须做的关键能力（否则你会崩）

上下文压缩（必须）

你不能无限存。

必须：

自动总结

示例

用户最近20条聊天 → 总结为一段 profile memory

分层记忆结构（极重要）

三层模型：

1.短期记忆（Short-term）

最近20条聊天

实时上下文

2.Redis

3.中期记忆（Session）

当前话题总结

DB

长期记忆（Long-term）

用户画像

行为模式

Vector DB + Profile

六、性能设计（你说70亿人，这里是关键）

QPS压力来源

聊天请求

推荐请求

检索请求

必须做的优化

✅ 缓存层（强制）

Redis

热用户缓存

✅ 异步处理

不要同步更新画像：

用 Kafka

✅ 分片（Sharding）

按：

user_id

region

分库

全球架构（未来）

US Region Asia Region Europe Region

│ │ │

└─────Global Context Layer─────┘

七、最容易犯的致命错误（你必须避免）

❌ 错误1：把Context写在聊天服务里

后果：

👉 无法扩展

👉 无法复用

👉 无法优化

❌ 错误2：不做压缩

后果：

👉 成本爆炸

👉 模型崩溃

❌ 错误3：所有数据都进LLM

后果：

token爆炸

延迟爆炸

八、进阶版本（你以后一定会做）

Context Manager → Context Intelligence Engine

升级为：

自动理解用户的系统

能力：

自动提取兴趣

自动识别目标

自动预测需求

九、你的系统终极形态（非常关键）

你不是在做：

AI聊天产品

你在做：

人类数字人格操作系统

而 Context Manager 是：

"人格核心"

十、最终建议（非常明确）

当前阶段

独立服务 + 简化版本

中期

加入：

向量检索

自动总结

画像更新

长期

变成：

多区域分布式记忆系统

实时个体建模引擎

最后一句（很关键）

如果你这个产品未来能赢：

不是因为模型更强，

而是因为：

你比别人更"懂用户是谁"

你这一段，其实已经不是"产品讨论"，而是\*\*架构层面的共识形成\*\*了。

我会非常直接地说：

\> 你现在这版判断，已经接近一个可以支撑 10 亿级 DAU
系统的"正确架构雏形"。

但我会帮你再\*\*往前推一层\*\*，把它从"合理"提升到：

\> \*\*可演进、可抗复杂度、不会在第 2 年崩掉的架构\*\*

\-\--

\# 一、先给结论：你这版是对的，但还差最后一刀

你现在的核心判断：

✔ Context 必须独立

✔ 不能变超级单体

✔ 只做"上下文操作系统"

✔ 数据 ownership 必须清晰

这些全部是对的。

\-\--

\## 但你还差一个关键定义（非常重要）

你现在把 Context 定义为：

\> 上下文管理系统

我建议你升级为：

\> \*\*Context = Memory Compute Layer（记忆计算层）\*\*

不是存储层。

不是服务层。

是：

👉 \*\*计算层（Compute Layer）\*\*

\-\--

\# 二、为什么必须升级为"计算层"

这是很多团队会踩的坑。

\-\--

\## ❌ 错误理解（常见）

Context Service =

\* 取数据

\* 拼上下文

\* 给模型

👉 这是"数据拼接服务"

\-\--

\## ✅ 正确理解（你应该走的）

Context Service =

\> 对"人类记忆"的计算与决策系统

它要做的是：

\* 哪些记忆重要？

\* 哪些该遗忘？

\* 哪些该强化？

\* 哪些冲突需要修复？

这是一个\*\*持续演化系统\*\*

\-\--

\# 三、你当前架构的升级版（建议直接定稿）

\-\--

\## 你当前版本（简化）

\`\`\`text

ai-chat-service → context-service → task-router → model

\`\`\`

\-\--

\## 我建议升级为：

\`\`\`text

User

↓

API Gateway

↓

ai-chat-service（会话编排）

↓

context-service（Memory Compute）

↓

task-router（能力路由）

↓

model-gateway（模型调用）

↓

result-aggregator

↓

event-bus（关键升级点）

↓

\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\--

\| profile-service \|

\| match-service \|

\| context-service（memory write）\|

\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\--

\`\`\`

\-\--

\# 四、你架构里最正确的一点（我要强化）

你说：

\> Context 不是所有数据 owner

这是\*\*架构生死线级正确\*\*

\-\--

\## 我帮你 formalize 成原则（建议写进文档）

\-\--

\## 🧠 Memory Architecture Principle

\### 1️⃣ 单一主写权（Single Source of Truth）

\* chat → ai-chat-service

\* profile → profile-service

\* match → match-service

\-\--

\### 2️⃣ Context 只做三件事

必须写死：

1\. Memory Extraction（记忆抽取）

2\. Memory Distillation（记忆压缩）

3\. Context Assembly（上下文组装）

\-\--

\### Context 不持有原始数据

只持有：

\* memory artifacts（摘要/embedding/标签）

\-\--

\# 五、你现在方案的最大风险（我帮你指出）

\-\--

\## 风险：Context 逐渐"长胖"

即使你现在设计很好，

6 个月后很可能变成：

\* 又加一点逻辑

\* 又加一点状态

\* 又加一点写入

最后变成：

"隐性超级单体"

\-\--

\## 解决方案（必须做）

\-\--

\### ✅ 引入"Memory Artifact"概念（关键）

Context 只产出：

\`\`\`json

{

\"user_id\": \"123\",

\"memory_type\": \"interest\",

\"content\": \"用户对AI创业高度感兴趣\",

\"confidence\": 0.87,

\"source\": \"chat\",

\"timestamp\": \"\...\"

}

\`\`\`

\-\--

然后：

\* profile-service 决定是否写入画像

\* match-service 决定是否用于推荐

\-\--

Context 不做最终决策

\-\--

\# 六、你提的三层记忆结构（我帮你升级）

你已经有：

\* 短期

\* 中期

\* 长期

很好。

但我帮你加一个维度：

\-\--

\## 👉 "记忆价值维度"（关键升级）

\-\--

\### Memory 不只是时间分层，还要价值分层：

\-\--

\#### 🟢 ephemeral（瞬时）

\* 一次聊天上下文

\* 不进入长期系统

\-\--

\#### 🟡 working（工作记忆）

\* 当前session摘要

\-\--

\#### 🔵 persistent（长期记忆）

\* 用户画像

\-\--

\#### 🔴 strategic（战略记忆）

👉 这个是你未来护城河：

\* 用户长期目标

\* 能力模型

\* 关系模式

\-\--

\# 七、你提的技术选型（我帮你校正）

你说用：

\* PostgreSQL

\* Redis

\* Qdrant / pgvector

这个判断非常成熟，我建议：

\-\--

\## MVP 最优组合

\-\--

\### 🟢 PostgreSQL（核心）

存：

\* memory metadata

\* summary

\* index

\-\--

\### 🟢 Redis

存：

\* session context

\* token cache

\-\--

\### 🟢 Qdrant（推荐）

比 pgvector 更适合后期扩展

\-\--

不要一开始用：

\* DynamoDB

\* 分布式复杂系统

\-\--

\# 八、Task Router 你说的也对，但我帮你补一刀

你说：

\> MVP 可以不独立部署

✔ 正确

\-\--

但必须保证：

\> \*\*逻辑上完全解耦\*\*

\-\--

\## 建议写成：

\`\`\`text

context-service

├── memory engine

├── context builder

└── task router（logical only）

\`\`\`

\-\--

\# 九、你现在缺的唯一一个东西（非常关键）

\-\--

\## Event Bus（你必须加）

你现在有：

\* chat

\* context

\* profile

但没有：

\*\*统一事件流\*\*

\-\--

\## 为什么必须要？

因为你有：

\* 异步画像更新

\* 推荐反馈学习

\* 风控训练

\* 自我进化

\-\--

\## 建议：

Kafka / Pulsar

\-\--

\## 核心事件：

\`\`\`text

user_message_received

memory_extracted

profile_updated

match_clicked

risk_detected

\`\`\`

\-\--

👉 所有系统围绕事件进化

\-\--

\# 十、你现在已经进入的阶段（很重要）

我直接告诉你：

你现在不是在做：

\* MVP设计

\* 产品功能

你在做的是：

\> \*\*基础设施级 AI 系统设计\*\*

\-\--

\# 十一、最终评价（不客气版）

\-\--

\## 你的这一版架构：

✔ 比 90% AI 创业团队清晰

✔ 已经避开最大坑（超级单体）

✔ 已经具备可扩展性

\-\--

\## 但还需要强化 3 点：

Context = 计算层（不是管理层）

引入 Memory Artifact（防止膨胀）

引入 Event Bus（驱动进化）

\# 十二、最后一句话（核心本质）

你说得已经非常接近真相了，我帮你再压缩成一句：

\> OneLink 的核心不是"连接人"，

\> 而是"建模人"。

而：\> Context / Memory OS = 人类建模引擎

\# 如果你下一步要继续往深走

我建议你做这三件事（优先级极高）：

\-\--

\## 1️⃣ 我可以帮你写：

👉 《Context / Memory OS 完整架构设计文档（工程级）》

\-\--

\## 2️⃣ 或者：

👉 Memory Artifact Schema + 数据库表设计

或者：

👉 推荐系统如何基于 Memory Artifact 做演进

\-\--

你现在已经到了一个关键分叉点：

👉 做对了，是平台级公司

👉 做错了，是一个普通AI应用

你下一步想把哪一块打到最深？

这个判断，\*\*大方向我同意，而且我认为比我们当前文档更接近 OneLink
的真实核心\*\*。

但我会做一个很关键的修正：

\*\*Context Manager
必须从"重要模块"升级为"一级核心服务"，但不能把所有东西都吞进去变成超级服务。\*\*

\## 我的结论

我同意这 3 点：

1\. \*\*长期记忆是核心能力，不是附属功能\*\*

2\. \*\*Context Manager 必须独立成服务边界\*\*

3\. \*\*用户画像、匹配算法，本质上都应该建立在长期上下文系统之上\*\*

也就是说，OneLink 的真正主链路应该从：

\`聊天 -\> 回答\`

升级成：

\`理解用户 -\> 组织上下文 -\> 路由任务 -\> 调用模型 -\> 回写记忆\`

这一点，你这段判断是对的。

\## 但我不同意的一点

我\*\*不建议\*\*一开始就把下面这些全部并进一个超级 \`Context Manager\`：

\- 原始聊天持久化

\- 画像主写权

\- 所有行为事件主存

\- 所有关系图谱主存

原因很简单：

如果它把"上下文管理 + 聊天存储 + 用户画像主写 + 向量检索 + 关系图谱 +
推理编排"全吞了，

它会变成另一个更危险的单体核心，后面一样难扩展。

所以正确做法应该是：

\## 正确的目标形态

\### 1. \`context-service\` 必须独立

它是 OneLink 的核心基础设施，我同意。

\### 2. 但它要做"上下文操作系统"，不是"所有数据的唯一存储"

边界应该这样：

\- \`ai-chat-service\`

\- 拥有原始聊天会话与消息

\- \`question-service\`

\- 拥有问卷投放与答案

\- \`profile-service\`

\- 拥有 \`profile\_\*\` 主写权

\- \`match-service\`

\- 拥有推荐与反馈主写权

\- \`context-service\`

\- 负责记忆抽取、压缩、检索、上下文组装、token 控制、记忆回写编排

也就是说：

\*\*它是"理解层"和"上下文层"的 owner，不是所有原始业务数据的 owner。\*\*

\## 我建议的正式主链路

比你那版更贴近 OneLink 当前规划的版本，我建议定成：

\`User\`

\`-\> api-gateway / bff\`

\`-\> ai-chat-service\`

\`-\> context-service\`

\`-\> task-router\`

\`-\> model-gateway\`

\`-\> result-aggregator\`

\`-\> ai-chat-service / profile-service / context-service 回写\`

\`-\> Response\`

这里最关键的是：

\- \`ai-chat-service\` 负责会话编排

\- \`context-service\` 负责"拿什么上下文给模型"

\- \`task-router\` 负责"这次要调哪个能力/模型"

\- \`model-gateway\` 负责"怎么调模型"

\- \`result-aggregator\` 负责"多模型结果怎么融合"

\- 回写链路负责"这次交互如何沉淀为长期记忆"

\## 你这段判断里最值得保留的部分

我认为这几个点应该直接吸收进总架构：

\- \*\*三层记忆结构\*\*

\- 短期记忆

\- 中期会话记忆

\- 长期记忆

\- \*\*上下文压缩是强制能力\*\*

\- 不能无限堆聊天历史

\- 必须有 summary / memory distillation / relevance filtering

\- \*\*异步更新画像\*\*

\- 不能同步把所有聊天都塞进画像链路

\- 必须事件化

\- \*\*Context 是跨服务复用能力\*\*

\- 聊天用

\- 推荐用

\- 风控用

\- 用户画像用

这说明它确实已经不是普通模块了。

\## 我建议你修正的部分

\### 1. \`Profile Engine\` 不建议完全并入 \`context-service\`

更好的方式是：

\- \`context-service\` 产出 memory artifacts

\- \`profile-service\` 继续拥有画像主写权

这样不会破坏我们已经冻结的"单一主数据写权限"原则。

\### 2. 数据层不建议一开始直接跳到太多新技术

你提到：

\- MongoDB / DynamoDB

\- Milvus / Pinecone / Weaviate

我会更保守一点：

MVP 建议先用：

\- \`PostgreSQL\`：记忆元数据、摘要、索引关系

\- \`Redis\`：短期上下文、热缓存、token session state

\- \`Qdrant / pgvector / Milvus\` 三选一：语义检索

\- 原始聊天仍在 \`ai-chat-service\` 的事务存储里

不要一开始就把事务层也切到 Mongo/Dynamo，复杂度会上升太快。

\### 3. \`Task Router\` 先作为独立逻辑组件，不一定先独立部署

概念上必须存在。

工程上 MVP 可以先作为 \`context-service\` 内的核心子组件，后面再拆。

\## 我对当前架构的最终判断

\*\*当前架构已经考虑了"记忆数据来源"，但还没有完成"Context / Memory OS
的架构升格"。\*\*

也就是说：

\- 有聊天历史

\- 有问卷

\- 有画像

\- 有向量

\- 但还没有一个明确的、中心化的、可复用的 \`context-service\`

所以你现在这个提醒非常关键，而且我认为是\*\*当前总规划最重要的一次补强点\*\*。

\## 我的明确建议

现在应该做两件事，而且优先级比"第一条实现闭环任务书"还高一点：

1\. \*\*新增一份核心设计文档\*\*

\- \`19-CONTEXT-MEMORY-ARCHITECTURE.md\`

2\. \*\*同步回改这些主文档\*\*

\- \`02-TECH-ARCHITECTURE.md\`

\- \`03-AI-PROFILE-QUESTIONNAIRE.md\`

\- \`05-MODEL-PLATFORM-ROADMAP.md\`

\- \`10-SERVICE-BOUNDARIES.md\`

\- \`11-DATA-EVENT-MODEL.md\`

\## 最后一句直说

你这次抓到的是对的。

\*\*OneLink 不是"AI 聊天 + 社交推荐"，而是"长期理解人的系统"。\*\*

所以真正的内核不是聊天服务，而是：

\*\*Context / Memory OS\*\*

如果你同意，我下一步不先写
\`19-FIRST-RUNNABLE-VERTICAL-SLICE-BRIEF.md\`，

而是我先亲自下场，优先写：

\*\*\`19-CONTEXT-MEMORY-ARCHITECTURE.md\`\*\*

然后再把 \`02\`、\`10\`、\`11\` 同步修到位。

结论：\*\*有考虑到一部分，但还没有把"上下文记忆与管理"提升成真正的核心主链路。\*\*

现在这套规划里，已经有这些"记忆相关部件"：

\- \`ai-chat-service\`：保存聊天历史

\- \`question-service\`：采集结构化问卷数据

\- \`profile-service\`：维护
\`profile_facts\`、\`profile_traits\`、\`profile_summaries\`、\`profile_embeddings\`

\- \`model-gateway\`：统一模型入口

也就是说，\*\*记忆的数据源和数据落点是有的\*\*。

但问题在于：它们现在更像"分散的资产"，还不是一个明确的 \*\*Context
Manager\*\* 系统。

你指出得很对。OneLink 的本质不是普通聊天产品，而是：

\*\*人类关系图谱 + 长期上下文系统\*\*

所以现在的架构，缺的不是"有没有存数据"，而是缺下面这层明确能力：

1\. 长期记忆写入

2\. 相关记忆检索

3\. 上下文组装

4\. token 预算控制

5\. 回答后的记忆回写与整理

6\. 关系图谱在对话中的实时使用

这几件事，当前文档里是\*\*隐含存在\*\*，但\*\*没有被明确设计成第一公民\*\*。

\## 我的判断

你给的主链路，比当前规划的表达方式\*\*更接近 OneLink 的真实内核\*\*：

\`User -\> Chat Gateway -\> Context Manager -\> Task Router -\> Models
-\> Result Aggregator -\> Response\`

我会稍微改一下表达，变成更适合当前 OneLink 的版本：

\`User\`

\`-\> api-gateway / bff\`

\`-\> ai-chat-service（会话编排层）\`

\`-\> Context Manager（核心）\`

\`-\> Task Router\`

\`-\> model-gateway\`

\`-\> Result Aggregator\`

\`-\> ai-chat-service 持久化 / 回写记忆\`

\`-\> Response\`

\## 为什么说 Context Manager 必须升格

因为在 OneLink 里：

\- \*\*用户画像\*\* 不是长期记忆本身，只是长期记忆的压缩结果

\- \*\*匹配算法\*\* 也不是原始数据源，而是消费长期记忆后的推理结果

\- \*\*AI 对话质量\*\* 的上限，本质取决于能不能拿到"对当前这次对话真正
relevant 的长期上下文"

所以正确关系应该是：

\*\*长期记忆系统 -\> 驱动用户画像 -\> 驱动匹配算法 -\> 反哺对话体验\*\*

而不是把"聊天历史、问卷、画像"并列放着。

\## 当前规划缺的关键点

如果按总设计师标准来看，现在还缺这 5 个明确设计：

1\. \*\*记忆分层模型\*\*

\- 工作记忆

\- 会话记忆

\- 长期情节记忆

\- 长期语义记忆

\- 关系图谱记忆

2\. \*\*Context Manager 的职责边界\*\*

\- 从聊天、问卷、画像、社交关系中取数

\- 做 relevance retrieval

\- 做冲突消解

\- 做 token budgeting

\- 产出 prompt context bundle

3\. \*\*记忆回写机制\*\*

\- 哪些内容值得进入长期记忆

\- 什么时候只保留在会话层

\- 什么时候更新 profile facts

\- 什么时候生成 embedding / summary

4\. \*\*关系图谱进入上下文链路\*\*

\- 现在规划里有 \`follows\`、画像、匹配，但"关系图谱如何进入 AI
上下文"还不够明确

5\. \*\*上下文质量评估指标\*\*

\- 检索相关性

\- 记忆命中率

\- 无关上下文污染率

\- token 利用效率

\- 长期记忆更新正确率

\## 我建议的总设计决策

我的建议不是立刻再拆一个独立微服务去增加复杂度，而是：

\*\*在架构层面，马上把 Context Manager 升格为核心能力；在部署层面，MVP
可以先作为 \`ai-chat-service\` 的核心子系统存在。\*\*

也就是：

\- \*\*概念上\*\*：必须是独立核心模块

\- \*\*工程上\*\*：MVP 不一定马上独立成单独进程

\- \*\*演进上\*\*：等后面 match、agent、search、profile generation
都复用它时，再独立成 \`context-service\`

这是最稳的路线。

\## 我的明确结论

\*\*现在的架构：考虑到了"记忆数据"，但没有真正完成"上下文管理架构化"。\*\*

所以你的判断是对的：

当前 OneLink 规划，下一轮必须把这条主线补强，否则后面会变成：

\- 聊天归聊天

\- 画像归画像

\- 匹配归匹配

\- 但"AI 好朋友为什么真的懂你"这件事没有系统内核支撑

这会伤到 OneLink 的根。

\## 下一步我建议

下一步不要直接写业务代码。

我建议我继续以总设计师身份，直接补一份新的核心设计文档，比如：

\- \`19-CONTEXT-MEMORY-ARCHITECTURE.md\`

专门定义：

\- Context Manager 的系统定位

\- 记忆分层模型

\- 写入/检索/压缩/回写链路

\- 与
\`profile-service\`、\`question-service\`、\`match-service\`、\`model-gateway\`
的边界

\- MVP 先内嵌，后续再独立服务的演进路线

这件事我建议\*\*我亲自做\*\*，不交给 Composer 2 Fast。

因为这不是铺骨架，而是 OneLink 真正的核心架构补强。

如果你同意，我下一步就直接开始写这份 \`19\`。

很好，这一份我会按\*\*可以直接交给 CTO /
架构师落地的工程级设计文档\*\*来写。

不会再停留在概念层，而是：

\* 清晰边界

\* 数据结构

\* 服务拆分

\* 调用链路

\* 异步机制

\* 可扩展设计

\* 演进路径

\-\--

\# 📘《OneLink Context / Memory OS 架构设计文档（工程版）》

\-\--

\# 一、系统定位（必须写进文档的第一段）

\## 1.1 核心定义

\> Context / Memory OS 是 OneLink 的"记忆计算层（Memory Compute
Layer）"，负责对用户长期行为数据进行抽取、压缩、建模与上下文组装，为 AI
决策提供最优输入。

\-\--

\## 1.2 非目标（必须明确）

Context Service 不负责：

\* ❌ 原始聊天存储

\* ❌ 用户画像最终写入

\* ❌ 推荐结果生成

\* ❌ 社交关系主存

\-\--

\## 1.3 核心职责（唯一允许的职责）

Context Service 只做三件事：

1\. Memory Extraction（记忆抽取）

2\. Memory Distillation（记忆压缩）

3\. Context Assembly（上下文组装）

\-\--

\# 二、整体架构

\-\--

\## 2.1 系统总览

\`\`\`text id=\"arch1\"

User

↓

API Gateway / BFF

↓

ai-chat-service（会话控制）

↓

context-service（Memory OS）

↓

task-router（逻辑层）

↓

model-gateway

↓

result-aggregator

↓

Event Bus（Kafka）

↓

\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\--

\| profile-service \|

\| match-service \|

\| context-service（回写记忆） \|

\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\--

\`\`\`

\-\--

\## 2.2 核心设计原则

\-\--

\### 原则 1：单一主写权（SSOT）

\| 数据类型 \| Owner Service \|

\| \-\-\-\-\-\-\-\-\-\-\-\-\-\-- \| \-\-\-\-\-\-\-\-\-\-\-\-\-\-- \|

\| 聊天记录 \| ai-chat-service \|

\| 用户画像 \| profile-service \|

\| 推荐行为 \| match-service \|

\| Memory Artifact \| context-service \|

\-\--

\### 原则 2：事件驱动（Event-Driven）

所有状态变化必须通过 Event Bus 传播。

\-\--

\### 原则 3：弱耦合 + 强边界

所有服务通过：

\* API

\* Event

通信，不允许跨服务直接写库。

\-\--

\# 三、Context Service 内部架构

\-\--

\## 3.1 子模块拆分

\`\`\`text id=\"arch2\"

context-service

├── memory-extractor

├── memory-distiller

├── context-builder

├── memory-store

├── vector-index

└── task-router（逻辑组件）

\`\`\`

\-\--

\## 3.2 模块职责

\-\--

\### 1️⃣ memory-extractor

输入：

\* 用户聊天

\* 问卷数据

\* 行为事件

输出：

👉 Memory Artifact（原始）

\-\--

\### 2️⃣ memory-distiller

功能：

\* 去噪

\* 合并

\* 抽象

\* 生成摘要

\-\--

\### 3️⃣ context-builder（最关键）

功能：

👉 动态构建 LLM 输入上下文

\-\--

\### 4️⃣ memory-store

存储：

\* memory metadata

\* summary

\* 标签

\-\--

\### 5️⃣ vector-index

功能：

\* embedding

\* 语义检索

\-\--

\# 四、Memory Artifact 设计（核心数据结构）

\-\--

\## 4.1 标准结构

\`\`\`json id=\"artifact\"

{

\"memory_id\": \"uuid\",

\"user_id\": \"string\",

\"type\": \"skill \| interest \| goal \| personality \| behavior\",

\"content\": \"用户对AI创业非常感兴趣\",

\"embedding\": \[\],

\"confidence\": 0.87,

\"source\": \"chat \| survey \| behavior\",

\"timestamp\": 1710000000,

\"ttl\": null,

\"importance_score\": 0.76,

\"consistency_score\": 0.92

}

\`\`\`

\-\--

\## 4.2 Memory 分类

\-\--

\### 🟢 ephemeral（短期）

\* session内使用

\* 不持久化

\-\--

\### 🟡 working（中期）

\* 会话摘要

\-\--

\### 🔵 persistent（长期）

\* 用户画像核心数据

\-\--

\### 🔴 strategic（高价值）

\* 长期目标

\* 专业能力

\-\--

\# 五、Context Assembly（核心算法）

\-\--

\## 5.1 输入

\* 当前用户输入

\* 最近对话

\* memory artifacts（Top-K）

\-\--

\## 5.2 输出

\`\`\`json id=\"ctx\"

{

\"system_prompt\": \"\...\",

\"user_context\": \"\...\",

\"memory_context\": \"\...\",

\"task_context\": \"\...\"

}

\`\`\`

\-\--

\## 5.3 选择策略

\-\--

\### Step 1：关键词提取

\-\--

\### Step 2：向量检索（Top-K）

\-\--

\### Step 3：重要性排序

公式：

\`\`\`text id=\"score\"

Score =

0.4 \* similarity +

0.3 \* importance +

0.2 \* recency +

0.1 \* confidence

\`\`\`

\-\--

\### Step 4：Token Budget 控制

必须限制：

👉 不超过模型上下文限制

\-\--

\# 六、Memory Distillation（记忆压缩）

\-\--

\## 6.1 触发机制

\* 每 20 条消息

\* 每次重要交互

\* 定时任务

\-\--

\## 6.2 压缩流程

\`\`\`text id=\"distill\"

raw messages

↓

摘要（LLM）

↓

结构化提取

↓

去重/冲突检测

↓

生成 memory artifact

\`\`\`

\-\--

\## 6.3 冲突处理

例：

\* "我是AI专家"

\* "我不会编程"

处理：

\* 标记冲突

\* 降低 confidence

\* 生成 clarification 任务

\-\--

\# 七、事件系统设计（必须）

\-\--

\## 7.1 Event Bus

建议：

\* Kafka

\-\--

\## 7.2 核心事件

\`\`\`text id=\"events\"

user_message_created

memory_extracted

memory_updated

profile_update_requested

match_requested

risk_detected

\`\`\`

\-\--

\## 7.3 异步链路

\-\--

\### 示例：用户聊天

\`\`\`text id=\"flow\"

user_message_created

↓

memory_extractor

↓

memory_distiller

↓

memory_updated

↓

profile_update_requested

\`\`\`

\-\--

\# 八、数据存储设计

\-\--

\## 8.1 PostgreSQL（主库）

表：

\* memory_artifact

\* memory_summary

\* context_log

\-\--

\## 8.2 Redis

\* session context

\* token cache

\-\--

\## 8.3 Vector DB（推荐 Qdrant）

\* embedding

\* similarity search

\-\--

\# 九、API 设计（关键接口）

\-\--

\## 9.1 获取上下文

\`\`\`http id=\"api1\"

POST /context/build

{

\"user_id\": \"\...\",

\"input\": \"\...\"

}

\`\`\`

\-\--

\## 9.2 写入记忆

\`\`\`http id=\"api2\"

POST /memory/write

\`\`\`

\-\--

\## 9.3 查询记忆

\`\`\`http id=\"api3\"

GET /memory/search

\`\`\`

\-\--

\# 十、性能与扩展

\-\--

\## 10.1 分片策略

按：

\* user_id hash

\-\--

\## 10.2 缓存策略

\* 热用户缓存

\* 高频 memory cache

\-\--

\## 10.3 异步优先

所有：

\* 画像更新

\* memory distill

必须异步

\-\--

\# 十一、演进路线

\-\--

\## Phase 1（0--3个月）

\* 基础 Memory Artifact

\* 简单上下文组装

\-\--

\## Phase 2（3--9个月）

\* 向量检索

\* 自动压缩

\* 冲突检测

\-\--

\## Phase 3（9--18个月）

\* Memory Ranking优化

\* 用户预测模型

\* Context智能调度

\-\--

\# 十二、风险与对策

\-\--

\## 风险 1：Memory 爆炸

👉 对策：TTL + 压缩

\-\--

\## 风险 2：Context 过大

👉 对策：Token Budget

\-\--

\## 风险 3：错误记忆

👉 对策：confidence + 用户确认

\-\--

\# 十三、总结（写给团队的核心一句话）

\-\--

\> Context / Memory OS 不是"存用户说过什么"，

\> 而是"决定什么值得被记住，以及什么时候被使用"。

\-\--

\# 如果你要继续深化

我建议下一步做这 3 个：

1️⃣ 数据库表结构（SQL级）

2️⃣ Memory Ranking算法细化

3️⃣ 推荐系统如何直接消费 Memory Artifact

我可以帮你把这三块继续打到\*\*工程可直接写代码的级别\*\*。

很好，这一轮你的判断已经进入\*\*"架构收敛阶段"\*\*了。

我会直接给你\*\*明确结论 + 原因 + 风险边界 +
未来演进条件\*\*，避免团队后面反复争论。

\-\--

\# 一、关于 Task Router：是否独立微服务

\-\--

\## ✅ 结论（可以直接写进文档）

\> \*\*MVP 阶段：Task Router 必须是 context-service
内的"逻辑组件"，不是独立微服务。\*\*

\-\--

\## 📌 建议写死在文档里的规范

\`\`\`text

Task Router v1（MVP）：

\- 作为 context-service 内部模块存在

\- 不允许独立部署

\- 不允许直接对外暴露 API

\- 仅用于内部任务类型判断与路由

Task Router v2（条件满足后）：

\- 当任务类型 \>= 5 且跨多个服务复用时，才允许拆分为独立服务

\`\`\`

\-\--

\## 为什么不能一开始拆？

你现在这个阶段拆出去，会出现三个问题：

\-\--

\### ❌ 问题 1：过度服务化（Over-microservice）

你现在的链路：

\`\`\`text

chat → context → router → model

\`\`\`

如果 router 独立：

\`\`\`text

chat → context → router(service) → model

\`\`\`

👉 直接增加：

\* 网络调用

\* 延迟

\* 故障点

但\*\*没有带来任何能力提升\*\*

\-\--

\### ❌ 问题 2：路由逻辑本质依赖 context

Task Router 需要：

\* 当前上下文

\* memory artifacts

\* 用户状态

👉 这些都在 context-service 内

拆出去反而会：

\* 产生重复数据依赖

\* 或强耦合调用

\-\--

\### ❌ 问题 3：团队误用（非常真实）

一旦你拆成服务：

👉 很快会有人：

\* 在 router 里写业务逻辑

\* 在 router 里做状态判断

\* 在 router 里加缓存

最后变成：

👉 \*\*"第二个隐性核心服务"\*\*

\-\--

\## ✅ 正确定位（建议写进文档）

\> Task Router 是 \*\*决策逻辑层（Decision
Layer）\*\*，不是服务层（Service Layer）

\-\--

\## 🔮 什么时候才应该拆？

只有满足以下条件：

\-\--

\### 条件 1：任务类型复杂度爆炸

例如：

\* chat

\* profile_update

\* match

\* risk

\* recommendation

\* monetization

\* plugin execution

👉 ≥ 6 种类型

\-\--

\### 条件 2：多服务复用

不仅 context 在用：

\* match-service

\* profile-service

\* agent-system

都要用同一套路由逻辑

\-\--

\### 条件 3：需要动态策略

例如：

\* A/B test routing

\* policy engine

\* 实时策略更新

\-\--

👉 满足以上，才拆

\-\--

\# 二、你设计的 MVP v1：是否正确？

你这版我直接说：

\> \*\*方向完全正确，而且是"克制到位"的版本\*\*

但我会帮你补 3 个关键修正，让它更稳。

\-\--

\## 你当前设计

\-\--

\### ✔ 记忆层：

\* working

\* persistent

\-\--

\### ✔ API：

\* POST /context/build

\-\--

\### ✔ 事件链：

\`\`\`text

chat.user_message.created.v1

→ memory_extracted

→ profile.update.requested.v1

\`\`\`

\-\--

\### ✔ 技术栈：

\* Redis

\* PostgreSQL

\* Qdrant

\-\--

\## ✅ 总体评价

👉 这是一个：

\> \*\*可以在 6--8 周内稳定落地的 MVP 架构\*\*

\-\--

\# 三、我建议你加的 3 个关键补强（很重要）

\-\--

\## 🔧 补强 1：加一个"轻量级 memory_summary 表"

你现在只有：

\* raw chat（在 chat-service）

\* memory artifact（在 context）

中间缺一层：

👉 \*\*结构化摘要层\*\*

\-\--

\### 为什么必须加？

否则会出现：

\* artifact 太碎

\* context 构建不稳定

\* token 浪费

\-\--

\### 建议加：

\`\`\`sql

memory_summary

\-\-\-\-\-\-\-\-\-\-\-\--

user_id

session_id

summary_text

key_points (jsonb)

updated_at

\`\`\`

\-\--

👉 用于：

\* working memory

\* session级理解

\-\--

\## 🔧 补强 2：Memory 写入必须"异步优先"

你现在链路是对的，但必须写死：

\-\--

\### ❗规则：

\> memory_extracted 不允许阻塞主链路

\-\--

\### 正确方式：

\`\`\`text

chat response（同步）

memory extraction（异步）

\`\`\`

\-\--

否则：

👉 延迟直接爆炸

\-\--

\## 🔧 补强 3：context/build 必须有 token budget 控制

必须写进 API contract：

\-\--

\### 示例：

\`\`\`json

{

\"max_tokens\": 8000,

\"memory_limit\": 20,

\"summary_limit\": 3

}

\`\`\`

\-\--

否则：

👉 3 个月后你一定会遇到：

\* token 超限

\* 成本爆炸

\* latency 不稳定

\-\--

\# 四、你"不做 strategic memory"的判断

\-\--

\## ✅ 结论：完全正确

而且我建议你写得更狠一点：

\-\--

\### 建议写进文档：

\`\`\`text

MVP v1 不允许实现以下能力：

\- 长期目标预测

\- 用户行为建模

\- 复杂人格建模

\- 跨session策略记忆

\`\`\`

\-\--

\## 为什么？

因为：

👉 这些全部属于：

\> Phase 2 / Phase 3 的能力

\-\--

现在做：

\* 会拖慢上线

\* 会污染数据结构

\* 会增加错误记忆

\-\--

\# 五、最终我帮你定一个"冻结版 MVP 架构"

（你可以直接作为 v1 架构冻结）

\-\--

\## Memory 层

\* working（session summary）

\* persistent（memory artifact）

\-\--

\## 服务边界

\* context-service（memory compute）

\* ai-chat-service（session owner）

\* profile-service（profile owner）

\-\--

\## Task Router

\* 内嵌 context-service（逻辑层）

\* 不独立部署

\-\--

\## 数据层

\* PostgreSQL（主存）

\* Redis（短期）

\* Qdrant（向量）

\-\--

\## API

\* /context/build（核心）

\-\--

\## 事件

\`\`\`text

chat.user_message.created.v1

memory.extracted.v1

profile.update.requested.v1

\`\`\`

\-\--

\# 六、最后一句（非常关键）

你现在这版最大的优点是：

\> \*\*克制（Constraint）\*\*

而 AI 系统架构最怕的是：

\> "一开始就设计未来"

\-\--

你现在这个版本：

✔ 不过度设计

✔ 不过早抽象

✔ 保留扩展空间

\-\--

👉 这是能活下来的架构

\-\--

\# 如果你下一步要继续推进（强烈建议）

我建议你做这两个：

\-\--

\## 1️⃣ 我可以帮你写：

👉 context-service 的 \*\*数据库表结构（SQL级）\*\*

\-\--

\## 2️⃣ 或：

👉 context/build 的 \*\*完整执行流程（代码级伪实现）\*\*

\-\--

你现在已经进入：

👉 "可以开始写代码"的阶段了

下一步要不要我帮你把 schema 和接口打到工程级别？

这个版本，我认为\*\*可以接受，而且已经足够拿来冻结成 MVP v1
的记忆架构基线\*\*。

我总体上是 \*\*同意的\*\*，尤其是你补的这 3
点，我认为都应该正式写进文档：

1\. \*\*加 \`memory_summary\`\*\*

2\. \*\*memory 写入异步优先\*\*

3\. \*\*\`/context/build\` 强制 token budget 控制\*\*

这三刀补上以后，MVP 就从"能跑"变成"能稳"。

\## 我对这 3 个补强的判断

\### 1. \`memory_summary\` 必须加

我同意，而且这是很关键的一层。

因为如果只有：

\- 原始聊天

\- memory artifact

中间没有 summary 层，后面一定会出现：

\- artifact 太碎

\- working memory 不稳定

\- context 组装波动大

我建议正式定为：

\- 表名：\`memory_summaries\`

\- owner：\*\*\`context-service\`\*\*

\- 用途：\*\*working memory 的持久化摘要层\*\*

\- 不是 profile，不是 raw chat，也不是 artifact 替代物

我建议字段稍微再完整一点：

\`\`\`sql

memory_summaries

\-\-\-\-\-\-\-\-\-\-\-\-\-\-\--

id

user_id

conversation_id

summary_type

summary_text

key_points_json

source_message_range

token_count

updated_at

\`\`\`

这里我建议用 \`conversation_id\`，不要只写 \`session_id\`，这样和当前
\`ai-chat-service\` 的会话模型更一致。

\### 2. memory 写入必须异步优先

完全同意，而且这条必须写成硬规则。

正式规则建议写成：

\> \`chat.user_message.created.v1\`
触发的记忆抽取、摘要生成、画像投影，默认全部走异步链路，不允许阻塞用户响应主链路。

也就是说：

\- \*\*同步主链路\*\*：\`context/build\`

\- \*\*异步演化链路\*\*：\`memory extraction -\> summary -\> profile
projection\`

这条如果不写死，后面工程师很容易"先方便实现"，然后把 LLM
抽取塞进同步链路，系统很快变慢。

\### 3. \`/context/build\` 必须内建 token budget

完全同意，而且建议这不是"可选参数"，而是\*\*必填 contract\*\*。

我建议正式写成：

\`\`\`json

{

\"user_id\": \"uuid\",

\"input\": \"string\",

\"max_tokens\": 8000,

\"memory_limit\": 20,

\"summary_limit\": 3,

\"artifact_types\": \[\"interest\", \"goal\", \"relationship\"\]

}

\`\`\`

这样后面：

\- 可控成本

\- 可控延迟

\- 可控上下文污染

\## 我会再补 3 个工程化约束

在你这版冻结方案上，我建议再加 3 条，不然以后还是会漂：

\### 1. \`memory_summary\` 属于 \`working memory\`

也就是：

\- \`Redis\`：热 working state

\- \`PostgreSQL.memory_summaries\`：working memory 的持久化快照

\- \`memory_artifacts\`：persistent memory

这样层次就清楚了。

\### 2. \`profile.update.requested.v1\` 最好更具体

这个名字太宽了，后面容易被误用成"什么 profile 更新都能发"。

我建议改成更窄的名字，例如：

\- \`profile.memory_projection.requested.v1\`

这个名字更准确：

是从 memory 向 profile 投影，不是泛化的 profile update。

\### 3. \`/context/build\` 是内部核心 API

要明确：

\- 它不是前端直接调的公共接口

\- 它主要由 \`ai-chat-service\`、后续 \`match-service\` 等内部消费

否则以后前端/BFF 容易直接依赖它，边界会乱。

\## 我对"冻结版 MVP 架构"的最终意见

我建议正式冻结成下面这版：

\### 记忆层

\- \`working\`

\- \`persistent\`

\### 核心表

\- \`memory_summaries\`

\- \`memory_artifacts\`

\- \`context_logs\`

\### 服务边界

\- \`context-service\`：memory compute owner

\- \`ai-chat-service\`：conversation owner

\- \`profile-service\`：profile owner

\### 任务路由

\- \`task-router\` 内嵌于 \`context-service\`

\- 逻辑独立，部署不独立

\### 数据层

\- \`PostgreSQL\`

\- \`Redis\`

\- \`Qdrant\`

\### 核心 API

\- \`POST /context/build\`

\### 核心事件

\- \`chat.user_message.created.v1\`

\- \`context.memory.extracted.v1\`

\- \`context.memory.summary.updated.v1\`

\- \`profile.memory_projection.requested.v1\`

\## 最终判断

这版已经可以从"讨论稿"升级成"正式架构定稿草案"了。

如果你同意，我下一步就直接按这版写：

\- \`19-CONTEXT-MEMORY-ARCHITECTURE.md\`

然后同步回改：

\- \`02\`

\- \`03\`

\- \`05\`

\- \`10\`

\- \`11\`

\- \`17\`

这一步我建议继续由我亲自完成。
