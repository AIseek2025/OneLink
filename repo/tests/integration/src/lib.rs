//! Chat → memory → profile 纵切面：**集成测试壳**（非主实现）。
//!
//! - 说明文档：`CHAT_MEMORY_PROFILE_SLICE.md`
//! - 可选 HTTP smoke：`cargo test -p integration_chat_memory_profile_slice -- --ignored` 或设置 `RUN_SLICE_HTTP_SMOKE=1` 运行非 ignored 测试（见测试源文件）。
