# contracts

- **`openapi/`** — 对外（经 BFF / 领域服务）HTTP 契约骨架，对齐 `OneLink/Rules/15-MVP-OPENAPI-DRAFT.md`。
- **`internal/`** — 不暴露给前端的内部能力（如 `model-gateway`）。

权威边界以 `OneLink/Rules/10-SERVICE-BOUNDARIES.md` 为准。
