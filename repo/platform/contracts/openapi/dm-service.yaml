openapi: 3.0.3
info:
  title: OneLink dm-service (draft skeleton)
  version: 0.1.0-draft
servers:
  - url: http://localhost:8080
paths:
  /api/v1/dm/threads:
    get:
      summary: placeholder
      responses:
        '501':
          description: Not implemented — skeleton only
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Placeholder'
    post:
      summary: placeholder
      responses:
        '501':
          description: Not implemented — skeleton only
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Placeholder'
  /api/v1/dm/threads/{threadId}/messages:
    get:
      summary: placeholder
      responses:
        '501':
          description: Not implemented — skeleton only
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Placeholder'
    post:
      summary: placeholder
      responses:
        '501':
          description: Not implemented — skeleton only
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Placeholder'
  /api/v1/dm/threads/{threadId}/read:
    post:
      summary: placeholder
      responses:
        '501':
          description: Not implemented — skeleton only
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Placeholder'
components:
  schemas:
    Placeholder:
      type: object
      description: Replace with Rules/15 schemas
